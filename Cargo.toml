[package]
name = "software-graphics"
version = "0.1.0"
edition = "2021"

[dependencies]
minifb = "0.24.0"
glam = "0.23.0"
gltf = "1.0.0"
image = { version = "0.24.6", default-features = false, features = ["jpeg"] }

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
